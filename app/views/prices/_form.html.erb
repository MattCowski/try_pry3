<%= form_for(@price) do |f| %>
  <% if @price.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@price.errors.count, "error") %> prohibited this price from being saved:</h2>

      <ul>
      <% @price.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :SFnumber_label %><br />
    <%= f.number_field :SFnumber %>
  </div>

 <!-- <div class="field">
    <%= f.label :multiplier_label %><br />
    <%= f.number_field :multiplier %>
  </div>  -->

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
